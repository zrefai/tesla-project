{
  "name": "utils",
  "version": "1.0.0",
  "description": "Utilities for the mono-repo",
  "author": "Zaki Refai",
  "license": "ISC",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "format": "npx prettier --write src",
    "build": "npm-run-all clean format && tsc && tsup"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.4"
  },
  "prettier": {
    "singleQuote": true
  }
}
